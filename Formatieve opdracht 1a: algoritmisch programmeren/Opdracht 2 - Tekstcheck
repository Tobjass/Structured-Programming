def tekstcheck(string1, string2):
    index = 0
    while True:
        if string1[index] != string2[index]:
            return index
        else:
            index += 1


print("Het eerste verschil zit op index: {}".format(tekstcheck(input("Geef een string: "), input("Geef een string: "))))
